#include <gtest/gtest.h>

#include "../Simulator/simulator.h"

TEST(TestCaseName, TestName1)
{
    fck::simulator s;
    //s.initialize();
    /*s.display_particle_position();
    
    size_t cycle_times = 200;
    while (cycle_times > 1)
    {
        
        s.simulate();
    }*/
    
}
